
```js
// a regular expression can be defined like this MAKE SURE IT HAS / ON EITHER SIDE!!

const expression = /\d+/; // finds any numbers in a string

expression.test(stringToTest); // returns a bool based on regex.
```